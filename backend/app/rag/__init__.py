# Marks `app.rag` as a package. RAG = Retrieval-Augmented Generation:
# we RETRIEVE relevant text from documents and AUGMENT the prompt with it,
# so the model answers from real sources instead of guessing.
